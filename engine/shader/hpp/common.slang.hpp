#pragma once

#include <math/type.hpp>

namespace cannele
{
    namespace descriptor
    {
        using BufferHandle = math::uint2;
        using TextureHandle = math::uint2;
        using SamplerHandle = math::uint2;
        using Sampler2DHandle = math::uint2;
    }
}
